/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["localhost"],
  },
};

module.exports = nextConfig;

// module.exports = {
//   images: {
//     domains: ["http://localhost"],
//   },
// };
